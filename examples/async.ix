use <std>
use <async>

pub def main() {
    var taskId = async::run(lambda (): void {
        async::sleep(300)
        std::println("Background task finished")
    })

    while (async::isRunning(taskId)) {
        std::println("Waiting...")
        async::sleep(100)
    }

    std::println("Done")
}
